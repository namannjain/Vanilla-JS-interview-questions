//intermediate
//eg in typescript there is type checking
//in js, name variabke should be string, but it can also be assgned a number
//what if we want to do checks and apply change only if its a string. Can be done by proxy deisgn pattern
//It is used internally in all latest frameworks and libraries
